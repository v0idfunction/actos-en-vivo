i.autoMIDI()
i.loop0=Looper(0);

i.loop0.rec(0);
i.loop0.start(0);

i.loop0.rec(1);
i.loop0.start(1);

i.loop0.rec(2);
i.loop0.start(2)

i.loop0.amp_(1,1);
i.loop0.amp(0,3);
i.loop0.rate(-4,2);
i.loop0.volume(0,0);
