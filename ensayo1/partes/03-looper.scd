i[\loop1]=Looper(0);
i[\loop1].rec;
i[\loop1].start;
i[\loop1].amp=16;
i[\loop1].rate=1;
i[\loop1].rate=1;
i[\loop1].rate=(-1);
i[\loop1][0].rate([-1,1/2,\r,\r]).do(1);
i[\loop1][1].rate([-2,\r,4,-2,-1,1/2]).speed(2).repeat(2);

i[\loop1].fx=i.synths.fx.time.choose;



i[\loop2]=Looper(0);
i[\loop2].rec;
i[\loop2].start;
i[\loop2][0].rate([-1,1.15,0.75,\r,\r]);
